# Public — backend API base URL (no trailing slash)
# Local dev:  http://localhost:5000/api/v1
# Production: https://lost-x-server.vercel.app/api/v1
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api/v1

# Server-only — must match ACCESS_TOKEN_SECRET in LostX-Server
ACCESS_TOKEN_SECRET=
